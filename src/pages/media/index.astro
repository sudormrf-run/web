---
import Layout from '../../layouts/Layout.astro';
import { useTranslations } from '../../i18n/utils';
import { getCollection } from 'astro:content';

const locale = 'ko';
const { t } = useTranslations(locale);

// Fetch archive items (자료 창고)
const archiveItems = await getCollection('archive', ({ slug }) =>
  slug.startsWith('ko/')
);
const sortedArchive = archiveItems.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
).slice(0, 3); // Show only 3 latest items

// Fetch AI news items (자동 발행 뉴스)
const ainewsItems = await getCollection('ainews', ({ slug }) =>
  slug.startsWith('ko/')
);
const sortedNews = ainewsItems.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
).slice(0, 3); // Show only 3 latest items

// Fetch podcast episodes (팟캐스트 라이브러리)
const podcastItems = await getCollection('podcasts', ({ slug, data }) =>
  slug.startsWith('ko/') && !data.isDraft
);
const sortedPodcasts = podcastItems.sort((a, b) => {
  const episodeDiff = (b.data.episodeNumber ?? 0) - (a.data.episodeNumber ?? 0);
  if (episodeDiff !== 0) return episodeDiff;
  return b.data.date.getTime() - a.data.date.getTime();
});
const featuredPodcasts = sortedPodcasts.filter((item) => item.data.isFeatured);
const leadPodcast = featuredPodcasts[0] ?? sortedPodcasts[0];
const secondaryPodcasts = sortedPodcasts.filter((item) => item.id !== leadPodcast?.id).slice(0, 3);
const podcastTags = [...new Set(sortedPodcasts.flatMap((item) => item.data.tags ?? []))].slice(0, 8);
const podcastChapterCount = sortedPodcasts.reduce((sum, item) => sum + (item.data.chapters?.length ?? 0), 0);
const podcastResourceCount = sortedPodcasts.reduce(
  (sum, item) => sum + (item.data.resources?.length ?? 0) + (item.data.relatedLinks?.length ?? 0),
  0
);
const getPodcastSlug = (slug: string) => slug.replace('ko/', '');
const getPodcastThumbnail = (item: (typeof sortedPodcasts)[number]) =>
  item.data.thumbnail ?? `https://img.youtube.com/vi/${item.data.videoId}/hqdefault.jpg`;

// YouTube & Social Links
const youtubeChannel = 'https://www.youtube.com/@sudoremove';
const instagramUrl = 'https://www.instagram.com/sudo.remove/';

// Essays & Columns data
const essays = [
  {
    title: '허깅페이스 르로봇 해커톤 후기',
    description: '허깅페이스의 르로봇 월드와이드 해커톤 서울 행사를 주최하며 경험한 과정과 결과',
    author: '박종현',
    date: '2025-06-24',
    link: 'https://sudormrf.run/2025/06/24/huggingface-lerobot-hackathon-review/',
    isExternal: true,
  },
];

// Presentations data
const presentations = [
  {
    title: 'VLA, Physical AI의 실현',
    speaker: '조준호',
    event: 'Physical AI Workshop',
    date: '2025-08-14',
    link: 'https://sudormrf-run.github.io/community/slides/20250814_vla_talk.pdf',
    type: 'pdf',
  },
  {
    title: 'HuggingFace의 Action Data Scaling',
    speaker: '박종현',
    event: 'AI Robotics Korea',
    date: '2025-08-15',
    link: 'https://jonhpark7966.github.io/public-slides/250815_airk',
    type: 'web',
  },
  {
    title: 'LLM 파인튜닝',
    speaker: '박종현',
    event: '교육 자료',
    date: '2024',
    link: 'https://github.com/jonhpark7966/public-slides/blob/main/250801_LLMFineTuning/LLM_FineTuning_250804.pdf',
    type: 'pdf',
  },
];
---

<Layout title={t('media.title')} locale={locale}>
  <div class="media-page">
    <div class="container">
      <!-- Header -->
      <header class="page-header">
        <h1 class="page-title">
          <span class="gradient-text">{t('media.title')}</span>
        </h1>
        <p class="page-subtitle">
          {t('media.subtitle')}
        </p>

      </header>

      <!-- Podcast Hero Section -->
      <section class="podcast-hero-section" aria-labelledby="podcast-hero-title">
        <div class="podcast-hero-copy">
          <div class="hero-kicker">SUDO REMOVE PODCAST</div>
          <h2 id="podcast-hero-title">
            AI와 로보틱스를 영상 단위로 다시 찾는
            <span>연구 라이브러리</span>
          </h2>
          <p>
            팟캐스트를 단순 재생목록이 아니라 요약, 챕터, 참고자료, 태그로 연결된
            지식 아카이브로 정리합니다. 궁금한 주제를 검색하고, 관련 링크를 따라가며,
            에피소드별 맥락을 빠르게 복습하세요.
          </p>

          <div class="podcast-actions">
            <a href="/podcasts/" class="primary-podcast-cta">팟캐스트 라이브러리 열기</a>
            {leadPodcast && (
              <a href={`/podcasts/${getPodcastSlug(leadPodcast.slug)}/`} class="secondary-podcast-cta">최신 에피소드 보기</a>
            )}
          </div>

          <div class="podcast-metrics" aria-label="팟캐스트 라이브러리 통계">
            <div><strong>{sortedPodcasts.length}</strong><span>Episodes</span></div>
            <div><strong>{podcastTags.length}</strong><span>Tags</span></div>
            <div><strong>{podcastChapterCount}</strong><span>Chapters</span></div>
            <div><strong>{podcastResourceCount}</strong><span>References</span></div>
          </div>

          {podcastTags.length > 0 && (
            <div class="podcast-tag-preview" aria-label="대표 팟캐스트 태그">
              {podcastTags.map((tag) => <span>#{tag}</span>)}
            </div>
          )}
        </div>

        {leadPodcast && (
          <div class="podcast-feature-panel">
            <a href={`/podcasts/${getPodcastSlug(leadPodcast.slug)}/`} class="featured-episode-card">
              <div class="featured-thumbnail">
                <img src={getPodcastThumbnail(leadPodcast)} alt={`${leadPodcast.data.title} 썸네일`} loading="eager" />
                {leadPodcast.data.duration && <span class="featured-duration">{leadPodcast.data.duration}</span>}
              </div>
              <div class="featured-content">
                <span class="featured-label">Featured Episode</span>
                <div class="featured-meta">
                  {leadPodcast.data.episodeNumber && <span>EP.{leadPodcast.data.episodeNumber}</span>}
                  <time datetime={leadPodcast.data.date.toISOString()}>
                    {leadPodcast.data.date.toISOString().split('T')[0]}
                  </time>
                </div>
                <h3>{leadPodcast.data.title}</h3>
                <p>{leadPodcast.data.summary?.[0] ?? leadPodcast.data.description}</p>
              </div>
            </a>

            {secondaryPodcasts.length > 0 && (
              <div class="recent-episode-list">
                <div class="recent-title">최근 에피소드</div>
                {secondaryPodcasts.map((item) => (
                  <a href={`/podcasts/${getPodcastSlug(item.slug)}/`} class="recent-episode-link">
                    <span>{item.data.episodeNumber ? `EP.${item.data.episodeNumber}` : 'Podcast'}</span>
                    <strong>{item.data.title}</strong>
                  </a>
                ))}
              </div>
            )}
          </div>
        )}
      </section>

      <div class="channel-strip" aria-label="소셜 채널 링크">
        <a href={youtubeChannel} target="_blank" rel="noopener noreferrer" class="channel-link">
          YouTube 채널 구독
          <span>매주 일요일 라이브</span>
        </a>
        <a href={instagramUrl} target="_blank" rel="noopener noreferrer" class="channel-link">
          Instagram 팔로우
          <span>최신 AI 뉴스 릴스</span>
        </a>
      </div>

      <!-- AI 데일리 뉴스 Section -->
      <section class="media-section">
        <div class="section-header">
          <div class="section-title-wrapper">
            <span class="section-icon ainews-icon">
              <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </span>
            <div>
              <h2 class="section-title">{t('media.news.title')}</h2>
              <p class="section-description">{t('media.news.description')}</p>
            </div>
          </div>
          <a href="/news" class="view-all-link ainews-view-all">
            전체 보기 →
          </a>
        </div>
        <div class="ainews-grid">
          {sortedNews.length > 0 ? (
            sortedNews.map((item) => {
              const slug = item.slug.replace('ko/', '');
              return (
                <a href={`/news/${slug}`} class="ainews-card">
                  <div class="ainews-card-content">
                    <div class="ainews-meta">
                      {item.data.hasHeadline && <span class="ainews-headline-badge">헤드라인</span>}
                      <span class="ainews-date">{item.data.date.toISOString().split('T')[0]}</span>
                    </div>
                    <h3 class="ainews-title">{item.data.title}</h3>
                    <ul class="ainews-summary">
                      {item.data.summary.slice(0, 2).map((line) => (
                        <li>{line}</li>
                      ))}
                    </ul>
                    {item.data.tags && item.data.tags.length > 0 && (
                      <div class="ainews-tags">
                        {item.data.tags.slice(0, 3).map((tag) => (
                          <span class="ainews-tag">#{tag}</span>
                        ))}
                      </div>
                    )}
                  </div>
                </a>
              );
            })
          ) : (
            <div class="ainews-empty">
              <p>아직 등록된 뉴스가 없습니다.</p>
            </div>
          )}
        </div>
      </section>

      <!-- Presentations Section -->
      <section class="media-section">
        <div class="section-header">
          <div class="section-title-wrapper">
            <span class="section-icon presentations-icon">
              <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"/>
              </svg>
            </span>
            <div>
              <h2 class="section-title">{t('media.presentations.title')}</h2>
              <p class="section-description">{t('media.presentations.description')}</p>
            </div>
          </div>
        </div>
        <div class="presentations-scroll">
          <div class="presentations-track">
            {presentations.map((pres) => (
              <a href={pres.link} target="_blank" rel="noopener noreferrer" class="presentation-card">
                <div class="presentation-icon-wrapper">
                  {pres.type === 'pdf' ? (
                    <svg width="32" height="32" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 3v6a1 1 0 001 1h6"/>
                    </svg>
                  ) : (
                    <svg width="32" height="32" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                    </svg>
                  )}
                  <span class="presentation-type-badge">{pres.type === 'pdf' ? 'PDF' : 'WEB'}</span>
                </div>
                <div class="presentation-info">
                  <span class="presentation-event">{pres.event}</span>
                  <h3 class="presentation-title">{pres.title}</h3>
                  <span class="presentation-speaker">{pres.speaker}</span>
                  <span class="presentation-date">{pres.date}</span>
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- 자료 창고 Section -->
      <section class="media-section">
        <div class="section-header">
          <div class="section-title-wrapper">
            <span class="section-icon archive-icon">
              <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
              </svg>
            </span>
            <div>
              <h2 class="section-title">자료 창고</h2>
              <p class="section-description">수도리무브 큐레이션, 우리가 보는 양질의 영상들</p>
            </div>
          </div>
          <a href="/archive" class="view-all-link archive-view-all">
            전체 보기 →
          </a>
        </div>
        <div class="archive-grid">
          {sortedArchive.length > 0 ? (
            sortedArchive.map((item) => {
              const slug = item.slug.replace('ko/', '');
              return (
                <a href={`/archive/${slug}`} class="archive-card">
                  <div class="archive-card-content">
                    <div class="archive-meta">
                      <span class="archive-source">{item.data.source}</span>
                      <span class="archive-date">{item.data.date.toISOString().split('T')[0]}</span>
                    </div>
                    <h3 class="archive-title">{item.data.title}</h3>
                    <p class="archive-summary">{item.data.description.split('\n')[0].replace(/^\d+\.\s*/, '')}</p>
                    <div class="archive-footer">
                      <span class="archive-duration">
                        <svg width="12" height="12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        {item.data.duration}
                      </span>
                      <span class="archive-chapters">
                        {item.data.chapters?.length || 0} chapters
                      </span>
                    </div>
                  </div>
                </a>
              );
            })
          ) : (
            <div class="archive-empty">
              <p>콘텐츠 준비 중...</p>
            </div>
          )}
        </div>
      </section>

      <!-- Essays & Columns Section -->
      <section class="media-section">
        <div class="section-header">
          <div class="section-title-wrapper">
            <span class="section-icon essays-icon">
              <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
              </svg>
            </span>
            <div>
              <h2 class="section-title">{t('media.essays.title')}</h2>
              <p class="section-description">{t('media.essays.description')}</p>
            </div>
          </div>
        </div>
        <div class="essays-grid">
          {essays.map((essay) => (
            <a href={essay.link} target={essay.isExternal ? "_blank" : undefined} rel={essay.isExternal ? "noopener noreferrer" : undefined} class="essay-card">
              <div class="essay-content">
                <h3 class="essay-title">{essay.title}</h3>
                <p class="essay-description">{essay.description}</p>
                <div class="essay-meta">
                  <span class="essay-author">{essay.author}</span>
                  <span class="essay-date">{essay.date}</span>
                  {essay.isExternal && (
                    <span class="essay-external">
                      <svg width="12" height="12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
                      </svg>
                    </span>
                  )}
                </div>
              </div>
            </a>
          ))}
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .media-page {
    padding: 2rem 0 4rem;
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    margin-bottom: 1rem;
  }

  .page-subtitle {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto 1.5rem;
  }

  .podcast-hero-section {
    position: relative;
    display: grid;
    gap: 2rem;
    margin-bottom: 1rem;
    padding: clamp(1.5rem, 4vw, 2.5rem);
    border: 1px solid rgba(67, 214, 168, 0.28);
    border-radius: var(--radius-xl);
    background:
      radial-gradient(circle at 12% 18%, rgba(67, 214, 168, 0.22), transparent 28%),
      radial-gradient(circle at 86% 8%, rgba(253, 115, 24, 0.14), transparent 24%),
      linear-gradient(135deg, rgba(17, 8, 17, 0.98), rgba(12, 18, 18, 0.96));
    overflow: hidden;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.28);
  }

  .podcast-hero-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(67, 214, 168, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(67, 214, 168, 0.05) 1px, transparent 1px);
    background-size: 42px 42px;
    mask-image: linear-gradient(135deg, black, transparent 72%);
    pointer-events: none;
  }

  @media (min-width: 980px) {
    .podcast-hero-section {
      grid-template-columns: minmax(0, 1.05fr) minmax(360px, 0.8fr);
      align-items: center;
    }
  }

  .podcast-hero-copy,
  .podcast-feature-panel {
    position: relative;
    z-index: 1;
  }

  .hero-kicker {
    display: inline-flex;
    margin-bottom: 1rem;
    padding: 0.3rem 0.7rem;
    border: 1px solid rgba(67, 214, 168, 0.35);
    border-radius: 999px;
    background: rgba(67, 214, 168, 0.1);
    color: var(--color-green);
    font-family: var(--font-mono);
    font-size: 0.76rem;
    letter-spacing: 0.1em;
  }

  .podcast-hero-copy h2 {
    max-width: 760px;
    color: var(--color-text-primary);
    font-size: clamp(2rem, 5vw, 4.4rem);
    line-height: 1.08;
    letter-spacing: -0.04em;
    margin-bottom: 1rem;
  }

  .podcast-hero-copy h2 span {
    display: block;
    color: var(--color-green);
    text-shadow: 0 0 26px rgba(67, 214, 168, 0.22);
  }

  .podcast-hero-copy > p {
    max-width: 680px;
    color: var(--color-text-secondary);
    font-size: 1.02rem;
    line-height: 1.85;
  }

  .podcast-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin: 1.5rem 0;
  }

  .primary-podcast-cta,
  .secondary-podcast-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 0.78rem 1rem;
    border-radius: var(--radius-md);
    font-weight: 800;
    text-decoration: none;
    transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .primary-podcast-cta {
    background: linear-gradient(135deg, var(--color-green), #2fbf95);
    color: #06110f;
    box-shadow: 0 10px 28px rgba(67, 214, 168, 0.22);
  }

  .secondary-podcast-cta {
    border: 1px solid rgba(67, 214, 168, 0.35);
    background: rgba(67, 214, 168, 0.08);
    color: var(--color-green);
  }

  .primary-podcast-cta:hover,
  .secondary-podcast-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 30px rgba(67, 214, 168, 0.18);
  }

  .podcast-metrics {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.75rem;
    max-width: 620px;
  }

  @media (min-width: 640px) {
    .podcast-metrics {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .podcast-metrics div {
    padding: 0.9rem;
    border: 1px solid rgba(67, 214, 168, 0.18);
    border-radius: var(--radius-lg);
    background: rgba(7, 16, 16, 0.68);
  }

  .podcast-metrics strong {
    display: block;
    color: var(--color-green);
    font-family: var(--font-mono);
    font-size: 1.35rem;
    line-height: 1;
  }

  .podcast-metrics span {
    color: var(--color-text-muted);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .podcast-tag-preview {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin-top: 1rem;
  }

  .podcast-tag-preview span {
    padding: 0.24rem 0.55rem;
    border-radius: 999px;
    background: rgba(253, 115, 24, 0.1);
    color: var(--color-accent-hover);
    font-size: 0.78rem;
  }

  .podcast-feature-panel {
    display: grid;
    gap: 0.9rem;
  }

  .featured-episode-card {
    display: block;
    border: 1px solid rgba(67, 214, 168, 0.3);
    border-radius: var(--radius-xl);
    background: rgba(17, 8, 17, 0.78);
    overflow: hidden;
    text-decoration: none;
    transition: transform 0.22s ease, border-color 0.22s ease, box-shadow 0.22s ease;
  }

  .featured-episode-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-green);
    box-shadow: 0 18px 42px rgba(0, 0, 0, 0.32), 0 0 24px rgba(67, 214, 168, 0.18);
  }

  .featured-thumbnail {
    position: relative;
    aspect-ratio: 16 / 9;
    background: var(--color-bg-secondary);
    overflow: hidden;
  }

  .featured-thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.22s ease;
  }

  .featured-episode-card:hover .featured-thumbnail img {
    transform: scale(1.035);
  }

  .featured-duration {
    position: absolute;
    right: 0.75rem;
    bottom: 0.75rem;
    padding: 0.2rem 0.45rem;
    border-radius: var(--radius-sm);
    background: rgba(0, 0, 0, 0.76);
    color: white;
    font-family: var(--font-mono);
    font-size: 0.75rem;
  }

  .featured-content {
    padding: 1.15rem;
  }

  .featured-label {
    color: var(--color-green);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .featured-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin: 0.55rem 0;
    color: var(--color-text-muted);
    font-size: 0.78rem;
  }

  .featured-content h3 {
    color: var(--color-text-primary);
    font-size: clamp(1.2rem, 3vw, 1.55rem);
    line-height: 1.35;
    margin-bottom: 0.55rem;
  }

  .featured-content p {
    color: var(--color-text-secondary);
    font-size: 0.9rem;
    line-height: 1.65;
  }

  .recent-episode-list {
    display: grid;
    gap: 0.55rem;
    padding: 1rem;
    border: 1px solid rgba(67, 214, 168, 0.18);
    border-radius: var(--radius-xl);
    background: rgba(7, 16, 16, 0.58);
  }

  .recent-title {
    color: var(--color-text-muted);
    font-size: 0.78rem;
    font-weight: 700;
  }

  .recent-episode-link {
    display: grid;
    gap: 0.2rem;
    padding: 0.65rem;
    border-radius: var(--radius-md);
    color: inherit;
    text-decoration: none;
    transition: background 0.2s ease;
  }

  .recent-episode-link:hover {
    background: rgba(67, 214, 168, 0.08);
  }

  .recent-episode-link span {
    color: var(--color-green);
    font-family: var(--font-mono);
    font-size: 0.7rem;
  }

  .recent-episode-link strong {
    color: var(--color-text-primary);
    font-size: 0.9rem;
    line-height: 1.35;
  }

  .channel-strip {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    margin-bottom: 4rem;
  }

  @media (min-width: 640px) {
    .channel-strip {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .channel-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1rem;
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-lg);
    background: rgba(17, 8, 17, 0.7);
    color: var(--color-text-primary);
    font-weight: 700;
    text-decoration: none;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .channel-link:hover {
    transform: translateY(-2px);
    border-color: var(--color-green);
  }

  .channel-link span {
    color: var(--color-text-muted);
    font-size: 0.78rem;
    font-weight: 500;
  }

  /* Essays Grid */
  .essays-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  }

  /* Essay Card - 보라색 테마 (에세이 아이콘과 통일) */
  .essay-card {
    background: var(--color-surface);
    border: 1px solid rgba(168, 85, 247, 0.3);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .essay-card:hover {
    border-color: #a855f7;
    transform: translateY(-4px);
    box-shadow:
      0 8px 24px rgba(0, 0, 0, 0.3),
      0 0 20px rgba(168, 85, 247, 0.4);
  }

  .essay-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    height: 100%;
  }

  .essay-title {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .essay-description {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    flex: 1;
  }

  .essay-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.5rem;
    padding-top: 0.75rem;
    border-top: 1px solid var(--color-border);
  }

  .essay-author {
    font-size: 0.8rem;
    font-weight: 600;
    color: #a855f7;
  }

  .essay-date {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .essay-external {
    margin-left: auto;
    color: var(--color-text-muted);
    display: flex;
    align-items: center;
  }

  /* Presentations */
  .presentations-scroll {
    overflow-x: scroll;
    margin: 0 -1rem;
    padding: 0 1rem;
    -webkit-overflow-scrolling: touch;
  }

  .presentations-scroll::-webkit-scrollbar {
    height: 10px;
  }

  .presentations-scroll::-webkit-scrollbar-track {
    background: var(--color-bg-secondary);
    border-radius: 5px;
  }

  .presentations-scroll::-webkit-scrollbar-thumb {
    background: var(--color-border-hover);
    border-radius: 5px;
    border: 2px solid var(--color-bg-secondary);
  }

  .presentations-scroll::-webkit-scrollbar-thumb:hover {
    background: var(--color-accent);
  }

  .presentations-track {
    display: flex;
    gap: 1.25rem;
    padding: 0.5rem 0 1rem;
  }

  /* Presentation Card - 파란색 테마 (발표 아이콘과 통일) */
  .presentation-card {
    flex-shrink: 0;
    width: 260px;
    background: var(--color-surface);
    border: 1px solid rgba(95, 173, 254, 0.3);
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .presentation-card:hover {
    border-color: var(--color-blue);
    transform: translateY(-4px);
    box-shadow:
      0 8px 24px rgba(0, 0, 0, 0.3),
      0 0 20px rgba(95, 173, 254, 0.4);
  }

  .presentation-icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.25rem;
    background: linear-gradient(135deg, rgba(95, 173, 254, 0.1) 0%, rgba(95, 173, 254, 0.05) 100%);
    color: var(--color-blue);
    position: relative;
  }

  .presentation-type-badge {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    background: var(--color-blue);
    color: white;
    font-size: 0.6rem;
    font-weight: 700;
    padding: 0.2rem 0.5rem;
    border-radius: var(--radius-sm);
  }

  .presentation-info {
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    flex: 1;
  }

  .presentation-event {
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--color-blue);
    text-transform: uppercase;
  }

  .presentation-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .presentation-speaker {
    font-size: 0.8rem;
    color: var(--color-text-muted);
  }

  .presentation-date {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    margin-top: auto;
  }

  .view-all-link {
    display: inline-block;
    margin-top: 1rem;
    color: var(--color-accent);
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9rem;
    transition: color 0.3s;
  }

  .view-all-link:hover {
    color: var(--color-text-primary);
  }

  /* Archive Section - 초록색 테마 (자료 창고) */
  .archive-icon {
    background: rgba(67, 214, 168, 0.15);
    color: var(--color-green);
  }

  /* Archive Grid - 3 columns on desktop */
  .archive-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .archive-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .archive-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .archive-card {
    background: var(--color-surface);
    border: 1px solid rgba(67, 214, 168, 0.3);
    border-radius: var(--radius-lg);
    padding: 1rem 1.25rem;
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .archive-card:hover {
    border-color: var(--color-green);
    transform: translateY(-2px);
    box-shadow:
      0 4px 16px rgba(0, 0, 0, 0.2),
      0 0 12px rgba(67, 214, 168, 0.3);
  }

  .archive-card-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .archive-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.7rem;
  }

  .archive-source {
    background: rgba(67, 214, 168, 0.15);
    color: var(--color-green);
    padding: 0.15rem 0.5rem;
    border-radius: var(--radius-sm);
    font-weight: 600;
    text-transform: uppercase;
  }

  .archive-date {
    color: var(--color-text-muted);
  }

  .archive-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .archive-summary {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .archive-footer {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-top: 0.25rem;
    padding-top: 0.5rem;
    border-top: 1px solid rgba(67, 214, 168, 0.3);
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .archive-view-all {
    color: var(--color-green);
  }

  .archive-view-all:hover {
    color: #5ee9b5;
  }

  .archive-duration {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .archive-chapters {
    color: var(--color-green);
    font-weight: 500;
  }

  .archive-empty {
    grid-column: 1 / -1;
    text-align: center;
    padding: 2rem;
    color: var(--color-text-muted);
    background: var(--color-surface);
    border: 2px dashed rgba(67, 214, 168, 0.3);
    border-radius: var(--radius-lg);
  }

  /* AI News Section - Orange theme (자동 발행 뉴스) */
  .ainews-icon {
    background: rgba(253, 115, 24, 0.15);
    color: var(--color-accent);
  }

  .ainews-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .ainews-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .ainews-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .ainews-card {
    background: var(--color-surface);
    border: 1px solid rgba(253, 115, 24, 0.3);
    border-radius: var(--radius-lg);
    padding: 1rem 1.25rem;
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .ainews-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow:
      0 4px 16px rgba(0, 0, 0, 0.2),
      0 0 12px rgba(253, 115, 24, 0.3);
  }

  .ainews-card-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .ainews-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.7rem;
  }

  .ainews-headline-badge {
    background: rgba(253, 115, 24, 0.15);
    color: var(--color-accent);
    padding: 0.15rem 0.5rem;
    border-radius: var(--radius-sm);
    font-weight: 600;
    text-transform: uppercase;
  }

  .ainews-date {
    color: var(--color-text-muted);
  }

  .ainews-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .ainews-summary {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .ainews-summary li {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    padding-left: 0.75rem;
    position: relative;
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
    margin-bottom: 0.25rem;
  }

  .ainews-summary li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  .ainews-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
    margin-top: 0.25rem;
  }

  .ainews-tag {
    font-size: 0.65rem;
    color: var(--color-text-muted);
    padding: 0.1rem 0.4rem;
    background: var(--color-bg-secondary);
    border-radius: var(--radius-sm);
  }

  .ainews-view-all {
    color: var(--color-accent);
  }

  .ainews-view-all:hover {
    color: #ff8c42;
  }

  .ainews-empty {
    grid-column: 1 / -1;
    text-align: center;
    padding: 2rem;
    color: var(--color-text-muted);
    background: var(--color-surface);
    border: 2px dashed rgba(253, 115, 24, 0.3);
    border-radius: var(--radius-lg);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .section-header {
      flex-direction: column;
    }

    .playlist-tabs {
      width: 100%;
    }

    .video-card {
      width: 280px;
    }

    .presentation-card {
      width: 240px;
    }
  }
</style>
